# 백엔드 검색 API base URL
VITE_API_BASE_URL=http://localhost:8000

# mock 모드 활성화 (true: 백엔드 없이 mock SSE 스트림 사용, false: 실백엔드 SSE 호출)
VITE_USE_MOCK=true

# (선택) 요청 타임아웃(ms) — 기본 30000. 실 MAS 백엔드는 합성까지 길어질 수 있어 상향 권장(예: 120000)
# VITE_REQUEST_TIMEOUT_MS=120000
